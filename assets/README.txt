Place the invitation image here as `invitation.jpg`.

Steps:
1. Save the attached image from the chat to this folder with the filename `invitation.jpg`.
2. Confirm the file path: `assets/invitation.jpg`.

After that you can preview the site by running:

python -m http.server 8000

and opening http://localhost:8000/index.html

If you want, tell me and I will embed the image directly into `index.html` as a base64 data URL so you don't need a separate file.